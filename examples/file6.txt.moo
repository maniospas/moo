/**/ SPACE1: x = const first value
/**/ SPACE1: y = const second value
/**/ SPACE1: z = const third value
/**/ pass The line below gets the resolved version of all variables of SPACE1 
/**/ pass and then runs them the result to bring the same variables to the global namespace
/**/ do {SPACE1: moolog}

/*** moolog ***/
/*** const {x} imported ***/
/*** const {y} imported ***/
/*** const {z} imported ***/